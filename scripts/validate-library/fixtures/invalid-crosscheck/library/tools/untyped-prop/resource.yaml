id: tool/untyped-prop
kind: tool
version: 1.0.0
summary: Cross-check fixture untyped-prop.
use_when:
  - testing validator cross-checks
facets:
  lifecycle: [build]
source: official
license: MIT
status: stable
implementation:
  module: src/core/capabilities/ok.ts
  export: register
surfaces:
  mcp:
    tool_name: summer_untyped
    remote: false
input_schema:
  type: object
  properties:
    foo:
      description: no type here
    bar:
      type: strang
  required: [baz]

authority:
  filesystem: false
  editor_mutation: false
  network: false
  credentials: false
  publish: false
