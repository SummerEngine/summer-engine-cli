id: tool/snapshot-diff
kind: tool
version: 1.0.1
summary: Diff two world snapshots into added/removed/changed nodes and count deltas — the structural receipt that a mutation did exactly what was intended.
use_when:
  - verifying a mutation batch against the snapshot_id taken before it
  - '"what changed in the scene after that script ran?"'
  - proving a batch only moved the props it was meant to
do_not_use_when:
  - no baseline snapshot exists — take summer_world_snapshot first
  - "requires an engine build with DiffWorldSnapshot (engine PR #156); on current shipped engines this returns engine_lacks_op"
facets:
  lifecycle:
    - build
  domains:
    - scenes
    - verification
  modalities:
    - scenes
compatibility:
  engine: ">=4.6"
related:
  tools:
    - tool/world-snapshot
    - tool/screenshot
  skills:
    - skill/verifying-scenes
    - skill/scene-scripting
source: official
license: MIT
status: preview
aliases:
  - summer_snapshot_diff
implementation:
  module: src/mcp/tools/perception-tools.ts
  export: registerPerceptionTools
surfaces:
  mcp:
    tool_name: summer_snapshot_diff
    remote: false
input_schema:
  type: object
  properties:
    from_id:
      type: string
      description: snapshot_id of the BEFORE snapshot (from summer_world_snapshot).
    to_id:
      type: string
      description: snapshot_id of the AFTER snapshot. Omit to snapshot the current state now and diff against that.
  required:
    - from_id
  additionalProperties: false
authority:
  filesystem: false
  editor_mutation: false
  network: false
  credentials: false
  publish: false
