# Golden for T6.1 — a 100x100 noise terrain with its HeightMapShape3D collider,
# a shadow-casting sun, an environment, a camera looking across it, save.
task: T6.1-hills
recorded_at: "2026-09-03T00:00:00.000Z"
description: ctx.terrain 100x100 (collider included), Sun DirectionalLight3D with shadows, environment, camera framing the terrain, saved
steps:
  - tool: summer_run_script
    args:
      source: |
        func run(ctx):
            var terrain = ctx.terrain(Vector2(100, 100), 6.0, 42, "Terrain")
            ctx.add_node("DirectionalLight3D", "Sun", null, {"rotation_degrees": Vector3(-45, 35, 0), "shadow_enabled": true, "light_energy": 1.2})
            ctx.ensure_environment()
            ctx.add_camera(Vector3(0, 45, 110), Vector3(0, 0, -10), true)
            var saved = ctx.save_scene()
            return {"saved": saved, "terrain": terrain != null}
