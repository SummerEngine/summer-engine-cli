# Golden for T1.4 — five unit cubes on the ground at the specified x, a sun, a
# camera (not asked for, harmless), save.
task: T1.4-layout-from-spec
recorded_at: "2026-09-03T00:00:00.000Z"
description: Cube_A..E at x=-4..4 resting on y=0, DirectionalLight3D Sun, saved
steps:
  - tool: summer_run_script
    args:
      source: |
        func run(ctx):
            var names = ["Cube_A", "Cube_B", "Cube_C", "Cube_D", "Cube_E"]
            for i in range(5):
                ctx.add_mesh("box", names[i], null, {"size": Vector3(1, 1, 1), "position": Vector3(-4 + i * 2, 0.5, 0)})
            ctx.add_node("DirectionalLight3D", "Sun", null, {"rotation_degrees": Vector3(-50, 30, 0), "shadow_enabled": true})
            ctx.add_camera(Vector3(0, 6, 12), Vector3(0, 0.5, 0), true)
            var saved = ctx.save_scene()
            return {"saved": saved}
