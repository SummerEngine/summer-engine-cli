# Factory (Droid) integration

Generated by `scripts/generate-registry` from `library/**/resource.yaml`:

- `registry/generated/plugin.factory.json` -> `.factory-plugin/plugin.json`
  (metadata manifest, now including the full skill list at
  `./library/skills/<slug>/` — the pre-v3 manifest listed zero skills, which
  was a bug fixed by generation; version stamped from `package.json`).

There is no `summer setup factory` target (`summer setup` rejects `factory`).
Install through the plugin marketplace instead:

```
droid plugin marketplace add https://github.com/SummerEngine/summer-engine-agent
droid plugin install summer@summer-engine --scope user
```

Factory reads MCP servers from the plugin's root `.mcp.json` (generated) and
skills ONLY from a root `skills/<name>/SKILL.md` directory; it has no `skills`
manifest field, so the array in `plugin.factory.json` is inert and this package
(skills under `library/skills/`) currently exposes no skills to Factory. Open
gap; fix is a root `skills/` layout, not a setup target.

Never edit the destination file by hand — edit `library/` resources or
`scripts/generate-registry/manifests.ts`, then run
`npm run generate:registry`. CI `--check` fails on any drift.
