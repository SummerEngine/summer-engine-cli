# Hardening review — 2026-09-02

Adversarial review of `v3-foundation` by six independent reviewers (cold-install e2e, core, mcp, registry/evals/CI, content, docs-vs-code) plus a resource.yaml audit and a template audit. Verdict in one line: **the gates verified that artifacts agree with each other, not that anything loads or works.** Findings below are ranked; each names the fix owner. Update STATUS.md when a row closes.

## P0 — blocks any publish
| # | Finding | Owner |
|---|---|---|
| 1 | Template pinning was documentation-only: `summer create` still cloned mutable default branches; no code read a manifest, verified a digest, or wrote `.summer/project.json`; built-in "self-pins" were placeholders; one template repo is private | fix-template-resolver |
| 2 | README Step 0: `doctor` returns `ok:true` with no skills/MCP/login installed → an agent following the playbook skips the install entirely; README cites a check id (`mcp-server`) that doesn't exist | docs pass + fix-host-surfaces (doctor semantics) |
| 3 | 359 cross-skill references use the v2 `summer:<category>/<name>` form (none resolve), all 50 relative reference links broken, 20 targets point at nonexistent skills, `make-game` dispatches to `summer:vfx` (doesn't exist) | cleanup-heldout |
| 4 | `using-summer` (the entry skill AGENTS.md says to start with) is `recommended:false` → `setup` never installs it; setup installs 44/83 silently | fix-host-surfaces |
| 5 | `index.json` tool records carry no MCP tool name, and drop `remote`/`authority` — agents can't map index → call; hosted-MCP design has no data | fix-registry-guards |
| 6 | Pre-commit hook matcher shape never fires in Claude Code/Cursor; OpenCode plugin points at the deleted `skills/` dir (zero skills) | fix-host-surfaces |
| 7 | `summer install` wipes an existing install before copying, no confirm; SIGTERM leaks the partial DMG. Unknown commands exit 0 with the marketing intro | fix-cli-bugs |
| 8 | `summer open <path>` broken whenever the engine isn't running (commander arg binding) | fix-cli-bugs |

## P1 — wrong, user-visible
- Login loop: validation errors and 4xx spin for 15 min; heartbeat hides the error. `gateway.url` config steers only login → tokens posted to prod. `gameSnapshot` double capture. Corrupt metadata leaves token on disk. `rebind()` returns stale identity. — fix-core-p1
- `withEngine` labels every validation throw as a transport failure ("may have partially applied"). Capability pre-flight has no override and depends on an engine table that lives on a different branch than the ops it guards. Three tool descriptors drifted from their zod (library-feedback missing `agent_model`, instantiate-scene `target_size`, screenshot `camera` framing). Feedback disclosure omits `session_id`; first-run notice is sent WITH the first event (contract says before). `summer_batch` contradicts itself on undo steps. Playbook step 0 leads with a tool no shipped engine has. — fix-mcp-seams
- Plan router matches substrings ("spaceship"→ship, "build"→ui). `summer run` spawn has no error handler. `summer logs` can never succeed. `summer agent` is a dev command in the public CLI. `summer tool get-agent-playbook` only redirects. Op-drift tripwire test silently never runs outside the monorepo (13 ops not in engine main). — fix-cli-bugs / fix-core-p1
- Validator accepts fiction (nonexistent implementation module, unregistered tool_name, `input_schema: {type:"banana"}`); schema forbids the external namespacing CONTRACT §4 promises; capability lint missed 35/58 smuggling probes; count-claims guard scans files with no counts; routing eval baseline stale (corpus 173 vs 180) and 1.0 is tuned on its own queries (~0.91–0.95 paraphrased) — fix-registry-guards + cleanup-heldout (held-out set)
- Authority lies: generate-image / creator-publish / library-feedback declare `filesystem:false` but write files; `remote` unset on 37 tools; 6 scripting tools `stable` while gated on unmerged ops; domains vocabulary split (scene/scenes, asset/assets); 54% of `related` one-directional; 49 tools with one-line use_when — queued: resource.yaml pass
- 5/13 integration READMEs wrong (factory not a setup target, kilo path, gemini manifest divergence, opencode config shape, scope mismatch); `.mcp.json` not generated; `engines >=18` vs Node-22 scripts + Node 18 URLSearchParams bug; dead deps `ai` + `@ai-sdk/anthropic` (12.9 MB) — fix-host-surfaces
- aliases.json has no runtime consumer; `.summer/` contract names files nothing writes; CONTRACT §2/§3/§5 claim "implementation in core, mcp no business logic, zod derived from input_schema" — false (64/70 tools implemented in src/mcp, dispatch mirrored in tool-dispatch.ts); three naming stories across docs; disclosure text in AGENTS/README/CONTRACT under-lists payload; README documents `summer update` (doesn't exist), omits `summer tool` — queued: docs/contract truth pass

## P2 — debt
Duplicate helpers (`asRecord` ×7, `sleep` ×3, `redact` ×2, 11 tool-dispatch↔mcp mirrors, 8× package.json require) → one consolidation pass after all fixers land. Dead exports. Repo-lint tests unlabelled. Test temp-dir leaks. Dev artifacts shipped in skill dirs (CREATION-LOG, test-pressure-*). Emoji in imported superpowers-lineage skills. Flag-name inconsistencies (`tool --json <args>`). Summercraft strings.

## Process finding
Six agents edited the same files in one shared worktree; three commits swept in other agents' staged files. Every "green" was a test asserting the mock it was written against. **A review sub-agent executed side-effecting CLI commands on the developer's machine** (logout, run, install) — review briefs must state read-only explicitly and forbid executing product commands with side effects.
