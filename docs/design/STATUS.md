# STATUS — where everything is, what actually works

Single page. If it isn't here, it isn't real. Updated 2026-09-03 (late night) — hardening complete; REAL e2e run done (create → live engine 0.5.65 → both faces; `E2E-2026-09-03.md`), its 18 findings fixed or routed; branch testable via `docs/TESTING.md`; navigation (`summer open`), the runtime librarian (`summer_search_library` / `summer_read_library` with the feedback footer), and an enforced tag vocabulary all landed. Later the same day the eight scene-scripting wave branches were integrated (`scene-scripting/v3-integration`, then rebased onto the E2E fix wave as `scene-scripting/v3-integration-rebased`; counts below reflect it; the merge record is `SCENE_SCRIPTING_INTEGRATION.md`). Findings ledger: REVIEW-2026-09-02.md. Companion: ROADMAP.md (sequencing), CONTRACT.md (rules).

## The one place
**`SummerEngine/summer-engine-agent` branch `v3-foundation`** (PR #18 open, NOT for merging yet). Everything toolkit-side lives here. Nothing has shipped: origin `main` and npm `summer-engine@2.8.2` are untouched. The scene-scripting waves (events, fabrication, perception, editor UI, runtime control, outcome evals, skill syncs) sit on top of it as `scene-scripting/v3-integration`, one merge commit per wave branch, pending review.

## 2026-09-09 — where the combined version is
- **Engine, complete:** `core/all-engine-work` @ `899605c3110` (0.5.66, 125 ops; + UI polish, Windows posture, legacy CLI retired, posture-based modals, release-warning guards; release-config build exit 0) = scene scripting + launch posture + deterministic clock + UTF-8 + Starcast (#147) + spatial suite (#158) + headless worker (#155) + Navigate. Built, harness **1115/0/4 + 1175/0/1**, toolkit skew warning **empty**. Local engine `main` points at it; `origin/main` untouched (clean fast-forward, +153). Pre-existing exit-time leak on SIGTERM (Summer singletons) noted in TK-VS-FOLD, not a regression. Worktree `summerengine-scripting`.
- **Toolkit, complete:** `v3-foundation` (local `main` = it), pushed. Proven against the complete engine through its OWN launch path (`summer run --bin --background`): discovery, context, scene-script-placed code running in the game, play/probe, world snapshot, all spatial + starcast + navigate ops, set-of-mark screenshots, doctor. Details: `TK-VS-FOLD-2026-09-07.md` (+ 09-09 addendum).
- **Compat proven (ship order):** the published 2.8.2 MCP server works against the new 0.5.66 engine, 10/10 calls clean → engine can ship before the toolkit. UI polish (Asset Store hidden in agent view, skippable intro video) folded into `core/all-engine-work` @ 74f5063 (built clean, not runtime-tested).
- **Not yet:** cold-model eval (needs `claude setup-token`), a human session, Windows launch posture, release build/Sparkle path, web PRs merge, engine UI polish branch `core/agent-view-polish` (AssetLib hidden in agent view, skippable intro video) in progress.

## What is verified working (how)
All rows re-verified after the 2026-09-02 fix wave; agent self-reports were NOT trusted — an HEAD presence audit + full gate run confirmed each.
| Thing | Verified by |
|---|---|
| Registry compiler → all root manifests, index, counts, aliases | `generate:registry --check` no drift; 23 compiler tests; CI parity step |
| Library: 209 resources validate (86 tools / 95 skills / 19 templates / 9 references — navigation entries added 2026-09-03, scene-scripting waves integrated the same day), capability lint clean (documented exceptions only), `remote` explicit on every tool, authority fields audited against implementations. 14 skills are `preview` (7 = source-cited intake from Marcus's gamedev-knowledge pipeline, 7 depend on unmerged engine ops); `summer setup` installs them labelled by default, `--stable-only` skips | `validate:library` + resource.yaml audit |
| `summer` CLI boots; `summer tool --list` (86); `summer skills list` (95); unknown commands exit 1; `summer open`/`run`/`install` safe paths | `npm run build` + smoke + 1382 tests (1376 pass; 6 skip without an engine build) |
| `summer_search_library` (BM25 + optional semantic fusion, lexical-only offline, never throws) and `summer_read_library` (full entry + feedback footer `entry_id@hash` — the reporting trigger) on both faces, engine-free | 64 tests; live probes: "make stylized water" → vfx-water-ripple #1, "take a screenshot of the game" → tool/screenshot #1 |
| `summer open <target>` / `summer_open`: intent → exact web page (login-gated via `/login?returnUrl=`) or editor surface (OpenScene/SelectNode/OpenResource/FocusDock/RevealInFileSystem); product map = web rows from summerengine.com's route catalog (vendored snapshot) + editor ids forwarded to the engine's `Navigate` op with availability from the engine advert (legacy ops on 0.5.65); reference rendered from the same data (`NAVIGATION-PLAN.md`, 2026-09-04) | 137 tests + scratch-HOME CLI smoke (`--print`); live editor targets pending an open project; engine `Navigate` PR unbuilt |
| 39 stable engine tools + 22 engine-free tools + 25 gated (preview) tools (the split `summer tool --list` shows; incl. `summer_starcast` from engine PR #147; `summer_frame_camera` + `summer_camera_visibility` removed 2026-09-03 — Marcus dropped them after benchmarks; the 15 scene-scripting wave tools added 2026-09-03 are all preview); one behavior per tool, CLI + MCP faces share the same functions; a preview tool on an old engine returns a structured `engine_lacks_op` (events tools: `engine_lacks_events`) result on BOTH faces | descriptor↔zod parity + mirror-parity tests; **live e2e on engine 0.5.65 with a real project (2026-09-02): project context, scene tree, viewport screenshot all worked through `summer tool`** — the wave tools have unit + parity coverage only, no live engine yet |
| Testable on this branch without publishing: `summer setup <agent> --local-dev` points the agent at this checkout's build; recipe in `docs/TESTING.md` (every command executed) | fake-HOME setup run + gate run |
| `summer_library_feedback` → live endpoint → Supabase table → /admin view | 29 MCP tests + 25 route tests; loopback wire proof; table verified live |
| Headless routing layer | 58 unit tests + fake worker; real-binary test SKIPS (no engine build) |
| Routing eval — tuning set (91 q): recall@5 1.0 / recall@1 0.81 / MRR 0.90. **Held-out set (42 q, written blind): recall@5 0.84 / recall@1 ~0.53 / MRR ~0.73** — up from 0.79/0.50/0.70 after the metadata wave, without touching the held-out queries | `npm run eval:routing` (gated) + `eval:routing:heldout` (report-only). Held-out is the real number. |
| Template pinning: `summer create` fetches the pinned SHA, verifies tree digest, refuses on mismatch, writes `.summer/project.json` | 84 tests + live fetch of a real template repo (digest matched) |
| Descriptor ↔ zod parity gate; validator cross-checks descriptors vs real registrations, implementation paths, schema shape | `npm test` — found and fixed 3 drifts on first run |
| Hooks fire in Claude Code/Cursor (matcher/if per docs); OpenCode plugin loads the library; `setup` installs all 95 skills incl. `using-summer` | hook smoke + opencode smoke test + cold install in fake HOME |
| **Real end-to-end run (2026-09-03, `docs/design/E2E-2026-09-03.md` + `e2e/` screenshots):** pinned `2d-platformer` → `summer create` (digest verified, independent recompute matched) → `summer run` (editor up in 3 s) → scene tree / add node / set prop / save (persisted to disk, both faces byte-identical) → screenshots → `summer_run_editor_script` (2.3 s, counts matched) → play / game frame / stop → `RunVerification` probe (both waypoints) → preview tools `engine_lacks_op` in ~100 ms → search/read/feedback → `summer open` (FocusDock receipt). 18 findings, all fixed on this branch except the engine-side and sign-off items listed below | driven by a real stdio MCP client + the CLI against a live editor; every quoted output in the report |
| **E2E fixes (14 commits, same night):** blank-frame detection (JPEG DC-coefficient scan, zero deps, tested on the real black/good frames) + one auto-recapture + honest captions; camera warning by scene kind (2D never warns about Camera3D); `get_diagnostics` is THE post-play read (console `_scope` note, skill reordered); `type_name` next to Variant ints; CLI face exits 1 on every result the MCP face marks `isError`; `get_project_context` one builder for both faces, pin surfaced at `projectMemory.pin`, 68,852 → 13,385 B default (`settingsPrefixes` arg for the rest); `doctor` grades stale skills as warning (exit 0) and recommends the `--local-dev` refresh when that is what is installed; MCP `instructions` in the handshake; preview entries × 0.8 in search (held-out unchanged); `dropped:true` now carries `status` + `reason`; fallbacks never name `summer_world_snapshot`; capture + fallback strings hoisted to one core module (mirror-parity pinned); template `2d-platformer` re-pinned to a fixed commit (HUD `%names`, control sizing, uid-free scenes, never-imported PNG loader) — pristine `run.sh` went FAILED(6) → exit 0 | full suite 1112 pass, `generate:registry --check` no drift, `validate:library` 0 errors, `eval:routing` PASS |
| **Navigation (2026-09-03, reworked 2026-09-04 — `NAVIGATION-PLAN.md`):** `tool/open` = `summer_open` MCP + `summer open <target>` CLI (+ `summer tool open`), one implementation (`src/core/capabilities/navigation/`). Web rows are generated from summerengine.com's route catalog (vendored snapshot `assets/navigation/web-routes.json`, 45 routes, `npm run sync:web-routes`); editor rows forward to the engine's one-table `Navigate` op and take availability from `/api/health capabilities.navigation` (on the shipped 0.5.65 engine, which predates the op, `scene`/`node`/`script`/`file` and the three docks still work through their original ops; the rest answer `engine_lacks_op`). `reference/product-map` is rendered from the same data (`npm run generate:product-map`); `skill/navigate-summer`. Login deep-links through `/login?returnUrl=`. `summer open <project-dir>` unchanged. Companion PRs: engine `feat/navigate-op` (Navigate op + table + chat-bridge refactor, NOT BUILT), web `feat/agent-routes-catalog` (`/agent-routes.json`) | 137 unit tests across navigation + CLI (resolution, ambiguity incl. "open settings", `open:false`, not-logged-in, engine-off, Navigate vs legacy forwarding, unsupported, snapshot parsing, rendered-reference parity) + descriptor↔zod parity + dispatch mirror + drift allowlist + CLI smoke under a scratch HOME. **Live-engine `Navigate` NOT yet exercised** (engine PR unbuilt). |

## What exists as code but CANNOT work yet (engine ops unmerged)
25 tools gated on unmerged engine features degrade with `engine_lacks_op` — the two events tools with `engine_lacks_events` — (override: `SUMMER_CAPABILITY_PREFLIGHT=off`):
scripting (run_script, world_snapshot, snapshot_diff, get_runtime_tree, inspect_runtime_node), spatial (4 of #158's six ops — frame_camera and camera_visibility dropped) + starcast, and the scene-scripting waves integrated 2026-09-03 — events channel (wait_for_event, recent_events, `summer events`), mesh fabrication (fabricate_3d), camera bookmarks (camera_bookmark; fixed-pose `summer_screenshot` framings), editor UI control (ui_actions, ui_tree, ui_activate, ui_screenshot), runtime control (game_probe, game_control, game_input, runtime_set, runtime_call, runtime_spawn, runtime_animate; the instance/mode/deterministic parameters of play and stop) — plus headless routing (flag `SUMMER_HEADLESS_ROUTING=1`) which needs the worker. `run_editor_script` ships today (corrected below).
Unblocked by: **SummerEngine/SummerEngine PR #155 (headless worker) + #156 (scene scripting) and the #156 follow-up commits on the engine's scene-scripting branch (waves I/K/L + the events channel) + #158 (Marcus's six-op spatial suite, with companion fixes) + #147 (starcast, Marcus's own PR)** — all rebase clean onto main; #147 and #158 conflict with each other in 13 files (whichever lands second re-rebases + regenerates the op registry). **None built by us.** Owners must build + smoke + merge.

## NOT yet done (honest)
- Cold-machine install test: RUN (tarball → fake HOME → setup → MCP handshake → 68 tools at the time, 86 today → engine-less calls). Mechanics passed; the P0s it found (doctor short-circuit, stale skill refs) are fixed; live-engine step still unverified (running editor exposed no API during the test).
- Independent adversarial review: RUN (6 reviewers). Findings + owners in REVIEW-2026-09-02.md; P0/P1 fixed; P2 debt scheduled (consolidation pass).
- Skill bodies: v2 references purged (359 invocations, 46 links, 20 dead targets); bare-slug cross-reference convention documented in docs/DEVELOPMENT.md.
- Held-out routing gap: 1.0 vs 0.84 — narrowing via metadata quality; 161 one-way `related` links surfaced as WARN (content backlog).
- `feat/templates-mcp-tool` dirty checkout (someone's uncommitted `with-engine.ts` + `summer_list_templates` work) — unreconciled with v3's `with-engine.ts` changes.
- Count guards now derive from counts.json (no literals).
- CONTRACT/AGENTS/README/CHANGELOG now match the code (truth pass 2026-09-02); planned-not-implemented is labelled as such: `.summer/state.json`/`decisions.ndjson`/`receipts/`, runtime alias resolution for skills/tools (aliases.json is generated, only template aliases resolve), Tier-1 1500-char feedback notes, Summer-aware side-loading. Folding the CLI/MCP mirror into shared capabilities: helpers deduped (one copy each, parity-tested); registration still happens twice by design for now.
- `evals/canary/` — Marcus's blind A/B tool-canary gateway, runnable (`npm run eval:canary`), records evidence, does not score yet.
- **Scripting today:** `summer_run_editor_script` (RunEditorScript) and `RunVerification` probes SHIP in engine 0.5.65 — agents can already run editor-side GDScript and runtime probe scripts. The checkpoint/rollback `summer_run_script` (RunSceneScript) + world snapshots need PR #156. (Corrected 2026-09-03: run-editor-script had been mislabelled as gated.)
- Live-engine verification of the 25 preview tools: impossible until engine PRs #155/#156 and the #156 follow-up commits merge (the engine-free tools and the 39 stable engine tools were live-verified on 0.5.65; the 15 wave tools added 2026-09-03 have never seen a live engine from this package — their op names are checked against the engine's feature-branch `op_registry.json` by `src/core/op-registry-drift.test.ts`, where the honesty half is EXPECTED to fail until the waivers are removed).
- `summer tool library-feedback` passes `reports` through without the MCP face's zod validation (invalid outcome → gateway `dropped:true` instead of a local error) — small, tracked.
- **Engine-side leftovers from the e2e run (chips left, engine repo):** `ViewportSnapshot` reads the SubViewport texture as-is (no forced draw / blank retry, unlike `ScenePreview`) → the black frame; `ScenePreview` receipt has no `scene_kind`; UTF-8 em dashes in ~26 C string literals are appended Latin-1 → mojibake (`no_rewind_reason`); first open rewrites `project.godot` features 4.6→4.7 + `.bak` + whole-scene rewrite (noisy `git status` on `--keep-git`); `run_editor_script` is non-rewindable by default (`checkpoint_capability_disabled`).
- **Focus stealing (2026-09-04, root-caused + fixed engine-side, unmerged):** every engine launch on macOS activates the app (`godot_application.mm` `activateIgnoringOtherApps:YES`, activation policy always Regular), so even `--summer-verify`/`--summer-offscreen` runs took the user's focus — a template verification batch launched the engine ~20× in 90 s and made the machine unusable. Engine fix on `fix/macos-no-focus-launch` (off main, not built) and `fix/macos-no-focus-launch-fold` (fold + fix, BUILT): `OS::SummerLaunchPosture` normal/background/invisible, new `--summer-background` (window, never activates until clicked), Accessory activation policy for non-normal postures, `create_process` children launched without activation, PlayGame `agent:true` child gets `--summer-background`, `/api/health` `capabilities.launchPostures: ["background","invisible"]` (macOS only). Toolkit (this branch, 4 commits): `summer run --background|--focus` (background by default when stdout is not a TTY), detection = `<engine> --help` probe for the literal flag → version fallback → health advert post-launch; `summer_play` quiet by default with `focus:true` opt-in, one `playGame` for both faces. **Not yet verified on a real launch** (needs one launch: `tests/1summer_engine/macos_focus_check.sh` in the engine fold worktree). Windows equivalent noted, not implemented.
- **Template truth pass (2026-09-04):** 14 repos fixed on `fix/truth-pass-2026-09-04` branches and re-pinned here (`TEMPLATES-FF-2026-09-04.md` has the fast-forward commands and preconditions). 2d-rpg re-pinned to a Dialogic return-path patch (1.0.2); runtime re-verification pending a launch window.
- `src/core/op-registry-drift.test.ts` reads the SIBLING engine checkout's `op_registry.json`; it goes red whenever that checkout is on a branch that adds an op (today: the primary engine checkout sits on `feat/navigate-op`, unbuilt, so `Navigate` looks implemented). Environmental, not a branch defect; the test should compare against `origin/main` of the sibling instead.
- Argument naming drift across tools (`path`/`nodePath`/`subjectPath`, `scenePath`/`scene_path`) — documented in E2E F-13, deferred (rename = breaking).
- **Pristine-boot sweep of all 17 pinned templates (`TEMPLATES-PRISTINE-BOOT-2026-09-03.md`): 16 of 17 fail the "Next step" `bash tests/autopilot/run.sh` on a project created by `summer create` and never opened in an editor.** Only the re-pinned `2d-platformer` passes. One root cause with three faces on 13 of them: a fresh checkout has no editor-built `.godot/` caches — `imported/*` (textures/fonts/audio/fbx → `Unable to open file …ctex` / `No loader found`), `uid_cache.bin` (every `ext_resource uid=` → WARNING), `global_script_class_cache.cfg` (every cross-file `class_name` → `Could not find type` … cascading to `Failed to compile depended scripts`) — and `--summer-verify` does no scan/import pre-pass. Two templates boot clean and fail only on the scaffold defaults (`PLAYER_PATH "Player"`, 2D pixel waypoints on 3D games). `SummerVerifyLogger` records WARNINGs as errors and `run.sh` fails on any. **Fixed in the scaffold (`b5dfe1f`, `64f54c8`; not 16 template patches):** `run.sh` runs `--headless --import` once when `.godot/global_script_class_cache.cfg` is missing (bounded, logged to `tests/autopilot/out/import.log`; measured 3–8 s, regenerates gitignored `*.import`; the batch-mode editor registers and removes its own `~/.summer/instances` entry), fails only on `ERROR`/`SCRIPT ERROR`/`SHADER ERROR` lines (WARNINGs printed + counted), fails closed without python3 (the old script silently exited 0), default run is a smoke test (boot, find the player by `PLAYER_PATH` → first `CharacterBody2D/3D` → `RigidBody` → `*player*`, 60 physics frames, 2 frames saved; waypoints opt-in), and `summer create` says what the first run does. Verified end-to-end on the control template (pristine → import 5 s → `PASSED: smoke test`; `--reimport`; old configured waypoints still pass). Post-import measurement on the OLD scaffold: third-person 606 → 0 error lines, brario 6 → 0, fps-npc 82 → 3 WARNINGs. **Still to run with the new scaffold (staged, waits for a launch window): brario, fps-old-school, city-kit, third-person, 2d-rpg, fps-npc.** Residual genuine template bugs (owners): fps-npc hand-written uids that never match generated imports + FBX arms texture; third-person case-mismatch asset paths; brario + fps-npc gitignore `*.import`; 2d-platformer `.gitignore` lacks `.godot/`. Also: 2 repos gitignore `*.import` while shipping assets; version/name drift on 14 manifests; `2d-grid-puzzle` is a 3D project labelled 2D; `2d-dungeon-roguelike` repo is private (installs only with local git credentials).
- Capability lint does not flag `git clone` / `cmake --install` / `hf download` as install commands — decide whether it should.
- P2 leftovers: 3 "Summercraft" strings in src/core/auth.ts + login.ts; embeddings sidecar not shipped yet (semantic search activates when `generate:registry --embed` runs with a provider and `/api/mcp/embed` exists on the gateway — web PR).

## Other places (dependencies, not homes)
- Web repo: **draft PR #331** `feat/library-feedback-mailbox` (endpoint + admin; 0 conflicts with main; prod table `library_feedback` already exists with the branch's 16 columns) — until merged, production answers `POST /api/mcp/library-feedback` with 404 and every report is `dropped:true, reason: endpoint_missing` (E2E F-03). Salt env set in Vercel. RLS fix applied.
- Web repo: PR #328 (open redirect via `next=`) and **PR #332** (`x-forwarded-host` trust on the OAuth callback; adversarial review SAFE — on Vercel the header is platform-set, so prod never had the hole; defense in depth) — both open, both need sign-off.
- Template repo `SummerEngine/template-2d-platformer`: fix branch `fix/hud-unique-names-and-docs` @ `ad9d6304` pushed; the toolkit pin targets that SHA. **Until `main` is fast-forwarded to it the pinned commit is reachable only via the branch — do not delete the branch.**
- Web cloud cleanup: task chip pending (not started).
- Engine PR #156 (`feature/scene-scripting` @ `1309cfc82`): the cloud scene-scripting session's 36 new engine commits are folded on **`origin/feature/scene-scripting-fold` @ `a5f1c7089c2`** (rebased with 0 conflicts — the cloud branch's first 35 commits were patch-identical to #156's; plus a regenerated `openapi.json` whose committed `--check` claim was false, plus 72 `String::utf8` wraps and the merge of `fix/utf8-op-strings`). **Built on the Mac 2026-09-03**: `scons platform=macos target=editor dev_mode=yes generate_bundle=yes` exit 0, zero warnings (werror), `bin/Summer.app` 0.5.65 (needs `thirdparty/sparkle` present; `use_ccache=` is not a variable in this fork — use `c_compiler_launcher=ccache`). **The e2e harness (`run_e2e.sh --native`, expected 1114/0/4; `--trust-boot` with Blender, expected 1174/0/1) has NOT been run — waits for a launch window.** Independent review of the 36 commits: nothing macOS-hostile, no local-API trust widening; for a human: `fabricate/bootstrap.py` is GPL-2.0 and embedded byte-for-byte in the editor binary, all commits authored `Claude <noreply@anthropic.com>`, a `/home/user/...` path is baked into `op_registry.json`, a new 750 ms post-write scene-reload hold is latency-visible, `tools/summer-cli` trips the phantom-op lint (warn only). Fast-forward: `git push origin a5f1c7089c2:refs/heads/feature/scene-scripting`.
- Engine `origin/fix/utf8-op-strings` @ `e908bda6bfa` (10 mojibake sites on `main`, 8 files; merge into `main` + rebuild). Engine PRs #155 / #158 / #147 unchanged. Engine local `main` has 5 stale unpushed commits — abandon.
- Docs repo `SummerEngine/docs` branch `scripting-summer` @ `0c2a1d2` (2 ahead, no PR) — merge after the toolkit ships (handoff order).

## Ship gates (all Mathias)
Engine: fast-forward PR #156 to `a5f1c7089c2` (after the e2e harness has run on the Mac build) and merge `fix/utf8-op-strings`; then drop the `KNOWN_UNIMPLEMENTED` waivers in `src/core/op-registry-drift.test.ts`, flip the 25 preview tools to stable, refresh counts here + CONTRACT + TESTING, then merge the docs branch. Fast-forward `template-2d-platformer` `main` → `ad9d6304` (keeps the pin reachable; current 2.8.2 users clone `main` and get the HUD fix). Merge web PR #331 (mailbox) before or with the toolkit publish, else feedback stays 404. PR #18 → main **together with** web mailbox PR #331 **and** npm publish 3.0.0; then rename repo → `summer`, org → `summerengine`; then web copy pass.
