# E2E run — 2026-09-03 — pinned template → live engine → both faces

Real product test of `v3-foundation` (commit `9a8b96e`) against the installed Summer Engine 0.5.65 on
a fresh project created from the pinned `2d-platformer` template. Every command below was run; every
quoted string is the exact output. Screenshots are in `docs/design/e2e/`. Nothing was fixed — this is
a report. Findings are ranked at the end; the "first-time user" narrative is last.

Legend: **PASS** = did what the docs/tool promise · **PARTIAL** = worked but with a wrong, misleading, or
undocumented side · **FAIL** = did not do what it promises.

## 0. Environment

| Item | Value | Source |
|---|---|---|
| Engine | Summer Engine **0.5.65** (`/Applications/Summer.app`, binary 142,819,216 B, `engine: "summer"`) | `Info.plist`, `health.version` |
| Toolkit | `summer-engine` **2.8.2**, `v3-foundation` @ `9a8b96e`, built with `npm run build` (`tsc`), run as `/opt/homebrew/bin/node dist/bin/summer.js` | `summer --version` |
| Node / OS | Node **v23.10.0**, macOS **26.6.2** (25G83), Apple M4 Pro (`OpenGL API 4.1 Metal - 90.5 - Compatibility`) | `sw_vers`, engine console |
| MCP SDK | `@modelcontextprotocol/sdk` 1.30.0, server `summer-engine 2.8.2`, 71 tools | `initialize`, `tools/list` |
| MCP client | `docs/design/e2e/mcp-drive.mjs` (stdio driver spawning `dist/bin/summer.js mcp`, cwd = project, `clientInfo summer-e2e-driver 1.0.0`) — not Claude Code (no `setup claude-code` was run: real `$HOME` was off limits) | this run |
| Account | logged in as `mathias_lange@hotmail.com` (real `~/.summer`, read-only from the test); `~/.summer/feedback-first-run` already existed (2026-09-01), so the first-run disclosure could not be exercised | `summer status` |
| Template pin | `template/2d-platformer` 1.0.0 → `https://github.com/SummerEngine/template-2d-platformer` @ `66fc71b8edcd1c7023b890c7c0ef7cc55d80748e`, `tree_digest 76ac4aee9a8a9d4d9ced0a3bc7b0cab76a4fc6eefd04403df967890c05a34c6c` | `library/templates/2d-platformer/resource.yaml` |
| Project | `<scratch>/summer-e2e-game` (left in place; `<scratch>` = `/private/tmp/claude-501/-Users-MathiasWork-development-publicsummerengine/9b218d40-25b2-4cde-ada3-1b8ec4883be6/scratchpad`) | — |
| Pre-conditions | `pgrep -fl Summer.app/Contents/MacOS/Summer` → none; `~/.summer/instances/` empty; no `api-token`/`api-port` | checked before launch |

Pre-engine baseline: `summer doctor` → **exit 1**, `✗ Skills up to date  skills v2.5.0 are significantly behind v2.8.2`
(the machine's `~/.claude/skills` marker is 2.5.0 from a published install). Everything else `✓`/`⚠ Local API engine not running`.

## 1. Step table

### Step 1 — `summer create` (pinned template)

| Command | Result | Verdict |
|---|---|---|
| `summer create 2d-platformer <scratch>/summer-e2e-game --keep-git` | exit **0**, 1.7 s. Output: `Fetching https://github.com/SummerEngine/template-2d-platformer at 66fc71b8edcd ...` / `Verified tree digest 76ac4aee9a8a` / `Source: … @ 66fc71b8edcd (tree digest verified)` / `Project created at …` / `Recorded template pin in …/.summer/project.json` / Next steps: `summer run …`, `bash …/tests/autopilot/run.sh   (verify the game without opening it)`, `Ask your agent to use the brainstorm-game skill to create .summer/GameSoul.md` | PASS |
| `cat .summer/project.json` | `{"template":{"id":"template/2d-platformer","version":"1.0.0","repo":"https://github.com/SummerEngine/template-2d-platformer","commit":"66fc71b8edcd1c7023b890c7c0ef7cc55d80748e","tree_digest":"76ac4aee9a8a9d4d9ced0a3bc7b0cab76a4fc6eefd04403df967890c05a34c6c"},"toolkit_version":"2.8.2","created_at":"2026-09-03T16:45:18.505Z"}` | PASS |
| `git rev-parse HEAD; git branch --show-current` | `66fc71b8edcd1c7023b890c7c0ef7cc55d80748e`, branch empty (detached) — `--keep-git` honoured | PASS |
| `git ls-tree -r 66fc71b8… --format='%(objectname) %(path)' \| shasum -a 256` (independent recompute) | `76ac4aee9a8a9d4d9ced0a3bc7b0cab76a4fc6eefd04403df967890c05a34c6c` — identical to the pin | PASS |
| What `create` added on top of the template (`git status --porcelain -uall`) | `?? .summer/project.json`, `?? tests/autopilot/README.md`, `autopilot.gd`, `probe_base.gd`, `run.sh` | PASS |
| Manifest vs content | Manifest summary: "2D platformer starter with TileMap levels, a CharacterBody2D controller, and parallax backgrounds." Actual `project.godot`: `config/name="2D Metroidvania Template"`, description "A Hollow Knight-inspired Metroidvania template with tight combat, ability-gated exploration, NPCs, quests, and boss fights." Geometry is `Polygon2D`, not TileMap. `template.json` in the repo says `"name": "2D Metroidvania Platformer"`, `"version": "1.0.1"` (manifest says 1.0.0); its `CLAUDE.md` says "Godot 4.5 fork" and "Only 3 rooms exist" while 7 room scenes ship. | PARTIAL (F-08) |

### Step 2 — `summer run` and health

| Command | Result | Verdict |
|---|---|---|
| `summer run <scratch>/summer-e2e-game` | exit **0**, 3.1 s. `Launching Summer Engine...` / `Summer Engine running (v0.5.65) on port 6550` / `  Project: 2D Metroidvania Template`. Engine wrote `~/.summer/instances/9ebf351b4ebadb09f897d04b2d7f5bb9.json` (`pid 81460, port 6550, engineVersion 0.5.65, publishesGlobal true`) plus `~/.summer/api-token` (64 B) and `api-port`. `pgrep` → `81460 /Applications/Summer.app/Contents/MacOS/Summer --path <project> --editor` | PASS |
| poll `summer tool is-running --args '{}'` | exit 0 on the **first** poll (API was up by the time `run` returned): `{"ok":true,"op":"IsGameRunning","playing":false,"scene":""}` | PASS |
| `summer tool get-project-context --args '{}'` (CLI face) | exit 0, **144,441 bytes**. `health`: `version "0.5.65"`, `ok true`, `pid 81460`, `port 6550`, `liveInstances 1`, `queueDepth 0`, `summerCloudEnabled false`. `health.capabilities`: `protocolVersion 1`, `preview.framings [auto,iso,top,front,back,left,right]`, `projectRead.prefixHonored false`, `sceneRead {defaultDepth 2, defaultLimit 200, targetedReadsHonorArgs true}`, `singleOnlyOps` (36 ops incl. `RunVerification`, `RunEditorScript`, `SaveScene`, `ViewportSnapshot`, `GameSnapshot`, Git*). **No `opKinds`**, no `capabilitySkewWarning`. `project.data.entries` = **969** `ProjectSettings` key/value pairs (83,140 B) — untrimmed; no `settingsTruncated`, no `guidance`, no `projectMemory` keys. | PARTIAL (F-04) |
| `summer_get_project_context` (MCP face, same engine state) | 24 ms, **88,803 bytes**. Keys: `boundProjectIdHash, currentScene, fileEditingGuidance, guidance, health, mainScene, project, projectMemory, projectName, projectPath, scene, summerUpdateNotice`. `project.data.entries` = **424** (`settingsTruncated true, totalSettings 969, settingsPrefixesIncluded [application/, display/, input/, physics/, rendering/]`). `projectMemory.files []`, `canonical.gameSoul null` — the template pin in `.summer/project.json` is not surfaced. `guidance`: "Use `summer_open_scene` with `mainScene` if no scene is open." | PARTIAL (F-04) |
| `summer doctor --json` / `summer doctor` | `"ok": false`, `summary {ok 9, warnings 0, failures 1}`; `local-api ok :6550 (0.5.65, 2D Metroidvania Template)`, `project-memory "0 files, 0 memory, 0 locked"`, `mcp-tools-list "71 tools registered"`; the one failure is `skills-version-stale` with `recommendedAction: "npx clear-npx-cache && npx -y summer-engine@latest setup claude-code --yes --force"`. Human form ends `1 issue to fix.` — **exit 1** with a fully working engine + MCP. | PARTIAL (F-09) |

### Step 3a — read the scene (both faces)

| Call | Result | Verdict |
|---|---|---|
| CLI `summer tool get-scene-tree --args '{"scenePath":"res://scenes/rooms/room_01.tscn","depth":2}'` | exit 0, 0.17 s, 6,157 B. `data.name "Room01"`, `class "Node2D"`, `activeTab "3D"` (on a 2D project), `limit 200`, `visited 37`; **38 nodes** with `class` (root + 12 children + 25 grandchildren). On-disk `.tscn` has 44 `[node` lines (depth-3 nodes such as `Geometry/Ground/Col` correctly excluded). | PASS |
| MCP `summer_get_scene_tree` same args | `initialize` 115 ms (`serverInfo {name "summer-engine", version "2.8.2"}`, caps `tools.listChanged, prompts.listChanged`, **no `instructions`**), `tools/list` 7 ms → 71 tools, call 94 ms, 6,157 B — byte-identical tree apart from `appliedThroughSeq` 11→12 | PASS |
| `inspect_node` with `{"scenePath":…,"nodePath":"Geometry/Ground"}` (my wrong guess) | CLI: `Missing required string argument: path`, exit 1. MCP: `MCP error -32602: Input validation error: Invalid arguments for tool summer_inspect_node: Required at path`, `isError true`. The schema is `{path}` only — no `scenePath`. | PASS (clear error) · F-13 |
| CLI `inspect-node --args '{"path":"Geometry/Ground"}'` | exit 0, 3,775 B: `node_name "Ground"`, `node_type "StaticBody2D"`, `props[]` as `{name, type, value}` with `type` a raw integer (`"type": 2`, `"type": 20`, …) | PASS (F-14) |
| MCP `summer_inspect_node {"path":"CamBoundTL"}` | 43 ms, 3,078 B, `node_type "Marker2D"` | PASS |
| Control: `inspect-node --args '{"path":"DoesNotExist"}'` | CLI prints `node not found: DoesNotExist` and **exits 0**. MCP returns the same text with `isError true`. | FAIL (F-06) |

### Step 3b — mutate, save, verify persistence (MCP face)

Calls in one MCP session (`docs/design/e2e/mcp-drive.mjs`), all `isError false`:

| # | Call | Result |
|---|---|---|
| 1 | `summer_add_node {"scenePath":"res://scenes/rooms/room_01.tscn","parent":".","type":"Label","name":"E2ELabel"}` | 145 ms. `results: [{op AddNode, ok, meta.applied true, nodePath "E2ELabel"}, {op SaveScene, ok}]`, `terminalState "applied"`, `requests 2` (the tool appended its own `SaveScene`; first receipt says `scenePersistence.attempted false`) |
| 2 | `summer_set_prop {…,"path":"E2ELabel","key":"text","value":"E2E 2026-09-03"}` | 49 ms. `appliedValue "E2E 2026-09-03"` + appended `SaveScene` |
| 3 | `summer_set_prop {…,"key":"position","value":"Vector2(200, 100)"}` | 43 ms. `appliedValue "(200.0, 100.0)"` + appended `SaveScene` |
| 4 | `summer_save_scene {"scenePath":"res://scenes/rooms/room_01.tscn"}` | 31 ms, `SaveScene ok`, `appliedSeq 21` (4 saves for 3 edits in total) |
| 5 | `summer_get_scene_tree {…,"depth":1}` | 25 ms. children = `[ParallaxBg, CamBoundTL, CamBoundBR, Geometry, Doors, Elder, Bench, Enemies, Coins, DeathPlane, HUD, PauseMenu, E2ELabel]`; `activeTab` now `"2D"` |

Disk: `room_01.tscn` sha256 `347bcceec4e0…` → `c45f6c1cd6e9…`, 44 → 45 `[node` lines, line 188: `[node name="E2ELabel" type="Label" parent="." unique_id=1184262088]` / `offset_left = 200.0` / `offset_top = 100.0` / `offset_right = 315.0` / `offset_bottom = 123.0` (a `Label` is a `Control`, so `position` is stored as offsets — correct). **Verdict: PASS.**

Side effects the engine wrote on open/save (`git diff --stat`): `scenes/rooms/room_01.tscn | 136 ++++----` (whole file rewritten: `uid="uid://…"` header, `unique_id=` on every node, the default `decoration_type = 0` dropped), `project.godot`: `config/features=PackedStringArray("4.6")` → `"4.7"`, `.gitignore`: `+# Summer Engine local-only state (SummerGit, caches, locks)` / `+.summer/local/`; untracked `project.godot.bak`, `.godot/…`, `assets/sprites/*.png.import`. (F-10)

### Step 3c — screenshots

| Call | Result | Verdict |
|---|---|---|
| MCP `summer_screenshot {"target":"viewport"}` @ 16:51:25Z | 44 ms, `image/jpeg` **22,067 B, 1072x1280 — entirely black** (`docs/design/e2e/01-mcp-viewport-black.jpg`). Text: `Editor viewport (1072x1280). Saved to …/viewport-1788454285282-s74eno.jpg. Review the image above and describe what you actually see.` No warning, no `failure_reason`. | FAIL (F-01) |
| CLI `summer tool screenshot --args '{"target":"viewport"}'` @ 16:52:32Z | exit 0, receipt `{"ok":true,"localPath":"…/viewport-1788454352476-6cssxf.jpg","width":1072,"height":1280,"bytes":47883,"metadata":{"source_surface":"editor_2d_subviewport_texture","capture_scope":"active_editor_viewport","os_screen_capture":false,"safety_boundary":"summer_owned_viewport_texture",…}}` — real content: room, HUD, `E2E 2026-09-03` label top-left (`03-cli-viewport-with-label.jpg`) | PASS |
| MCP viewport @ 16:54:49Z and CLI viewport @ 16:57:05Z | 30,610 B, 1280x768, real content, both captures md5-identical (`04-mcp-viewport-preplay.jpg`). **Black frame: 1 of 4 viewport captures**, the first one after the mutations flipped the editor from the 3D to the 2D tab. | — |
| MCP `summer_screenshot {"target":"scene","scenePath":"res://scenes/rooms/room_01.tscn"}` | 519 ms, 61,341 B, 1024x768, `framing: iso`. Text appended: `WARNING: this scene has no Camera3D — it will render grey/black when played.` and `NOTE: this preview is framed by a synthetic render camera, NOT the scene's own camera`. Image (`02-mcp-scene-render-pausemenu.jpg`) shows the **PauseMenu panel ("Paused / Take a breath, then choose your next move.")** drawn over the HUD — a CanvasLayer that `pause_menu.gd` hides in `_ready()`. A 2D scene never needs a Camera3D and this one plays fine (step 3e). | FAIL (F-05) |

### Step 3d — `summer_run_editor_script` (ships on 0.5.65)

Script (`docs/design/e2e/03d-count-nodes.gd`, a plain `func _run():` body): loads `res://scenes/rooms/room_01.tscn`, instantiates it, walks the tree counting `total_nodes` and nodes at depth ≤ 2, prints `E2E_COUNT …`.

| Result | Verdict |
|---|---|
| MCP call 2.3 s wall (`result.elapsed_ms 738`), `ok true, ran true, exit_code 0`, `output ["E2E_COUNT total_nodes=132 depth_le_2=39 root=Room01"]`, `errors []`, `boot_errors []`, `normalizations ["prepended \`extends EditorScript\` (the source had no extends line)", "prepended \`@tool\` (an EditorScript must be a tool script to run)"]`, `line_offset 2`, `out_dir <project>/.godot/summer_editor_script/81460_319916`. **`depth_le_2 = 39` = 38 (step 3a) + 1 (E2ELabel) → matches.** `total_nodes 132` = sub-scenes (enemies, coins, doors, HUD, NPC) expanded — expected for `instantiate()`. | PASS |
| Also returned: `checkpoint {ok false, reason "checkpoint_capability_disabled", error "No SummerGit checkpoint was taken because project rewind is disabled. This run is NOT rewindable."}`, `no_rewind_point true`, and `no_rewind_reason` containing `"The editor script still ran â these changes cannot be rewound…"` — bytes `c3 a2 c2 80 c2 94`, a UTF-8 double-encoded em dash. Tool description promises "tens of seconds"; it took 2.3 s on this project. | PARTIAL (F-11, F-15) |

### Step 3e — play, observe, stop (MCP face) + `RunVerification`

One MCP session, in order:

| # | Call | Result |
|---|---|---|
| 1 | `summer_screenshot viewport` | 139 ms, real frame 1280x768 (see 3c) |
| 2 | `summer_clear_console` | `{"message":"Console output cleared","ok":true,"op":"ClearConsoleOutput"}` |
| 3 | `summer_play {}` | 191 ms, `{"ok":true,"op":"PlayGame","playing":true,"scene":"main_scene","agent_quiet":false}` |
| — | sleep 3 s | |
| 4 | `summer_is_running` | `{"playing":true,"scene":"res://scenes/rooms/room_01.tscn"}` |
| 5 | `summer_screenshot {"target":"game"}` | 1,396 ms, `image/jpeg` **95,651 B, 1280x719** `Running game frame (1280x719)` — real gameplay frame: forest background, player, coins, enemy, doors, HUD, and the `E2E 2026-09-03` label from step 3b (`05-mcp-game-frame.jpg`). The tool description says `"game" … Not available over a plain local connection — needs the Summer desktop app bridge.` It worked over the local connection. (F-15) |
| 6 | `summer_get_console {}` | `summary {total 7, totalUnique 7, warnings 1, errors 0, std 6, editor 0}`, `messages` = **1** entry (`[url]scene/gui/control.cpp:3011[/url] - This control can't grab focus…`, warning); `_filter {totalIn 7, totalOut 1, droppedByLevel 6}` — default filter drops all `std` lines; `{"raw":true}` returns them (`Welcome to Summer Engine…`, `[SE] AuthManager: CLI credential found; desktop session exchange required`, `[SE] Sparkle: Updater initialized…`, …). (F-07) |
| 7 | `summer_screenshot viewport` (while playing) | `isError true`: `{"error":"Switch to 3D or 2D tab to capture scene viewport","failure_reason":"wrong_editor_context"}` — structured, honest |
| 8 | `summer_stop` | `{"ok":true,"op":"StopGame","was_playing":true}` |
| 9 | `summer_is_running` (+1.5 s) | `playing false`; CLI cross-check `summer tool is-running` → `{"ok":true,"op":"IsGameRunning","playing":false,"scene":""}` exit 0 |

After the session: `summer_get_debugger_errors` → `summary {errors 4, warnings 35, session_active false}`; the 4 errors are `Node not found: "%HealthLabel" (relative to "/root/Room01/HUD")` (+ `%ControlsHint`, `%CoinsLabel`, `%HeartsContainer`), callstack `res://scripts/ui/hud.gd @implicit_ready lines 4–7`. `summer_get_diagnostics` → `has_issues true`, `debugger.errors 4`, `guidance "Errors present. Check console.messages, debugger, and script_errors for details."` — so the ladder routes correctly, but `get_console` alone reported 0 errors. **Verdict: PASS for the tools.**

`RunVerification` probe (`summer_batch {"ops":[{"op":"RunVerification","probe_source":"<tests/autopilot/autopilot.gd verbatim>","max_seconds":40}]}`): 1.8 s, `ok true`, `results.finished true`, `duration_ms 1049`, `reports {mode "2D", player_path "Player", player_class "CharacterBody2D", start_pos "(200.0, 419.9913, 0.0)", waypoint_0_reached true, waypoint_0_pos "(264.5, 419.9913, 0.0)", waypoint_1_reached true, waypoint_1_pos "(563.1664, 499.9252, 0.0)"}`, `frames ["00_start.jpg","01_waypoint_0.jpg","02_waypoint_1.jpg"]` (1920x1080 JPEG, ~142 KB each, `<project>/.godot/summer_verify/81460_588134/`; downscaled copies `06-probe-00_start.jpg`, `07-probe-02_waypoint_1.jpg`). **`errors_seen`** = 6 lines: a `WARNING|scene/gui/control.cpp:1519|_set_size|Nodes with non-equal opposite anchors…` (2 lines) and the same 4 `ERROR|scene/main/node.cpp:1976|get_node|Method/function failed. Returning: nullptr|Node not found: "%HeartsContainer" (relative to "/root/Room01/HUD").` etc.

`bash tests/autopilot/run.sh` — the second "Next step" printed by `summer create` — exit **1**: `FAILED: 6 engine error(s) during the run`. Root cause is in the pinned template itself: `scripts/ui/hud.gd` lines 4–7 use `%HeartsContainer`, `%CoinsLabel`, `%ControlsHint`, `%HealthLabel`, but `scenes/ui/hud.tscn` (unchanged from commit `66fc71b8`) sets `unique_name_in_owner` on **no** node. Visible consequence in the frames: `+100` floating text on coin pickup while the HUD still reads `x 0`. **Verdict: FAIL for the template (F-02).**

### Step 3f — preview tools on this engine

| Call | Result | Verdict |
|---|---|---|
| MCP `summer_world_snapshot {"scene_path":"res://scenes/rooms/room_01.tscn"}` | 105 ms, `isError true`: `{"error":"This Summer Engine build doesn't support GetWorldSnapshot yet — read structure with summer_get_scene_tree (pass depth) and verify visually with summer_screenshot, or update Summer Engine (restart it after updating). Engine said: unknown op: GetWorldSnapshot","failure_reason":"engine_lacks_op","op_error":"unknown op: GetWorldSnapshot","op":"GetWorldSnapshot","terminalState":"failed","errorClass":"fatal"}` — the second shape from TESTING.md §f (engine advertises no `opKinds`, so the op was sent). No hang. | PASS |
| MCP `summer_test_placement {…,"subjectPath":"E2ELabel","candidateGlobalPosition":[0,0,0],"candidateGlobalRotationDegrees":[0,0,0]}` | 98 ms, `engine_lacks_op`, `op "TestPlacement3D"`, `op_error "unknown op: TestPlacement3D"`. Its fallback text says `read the subject and its neighbours with summer_inspect_node / summer_world_snapshot` — `summer_world_snapshot` is itself unsupported here. | PASS (F-16) |
| CLI `summer tool world-snapshot` / `test-placement` | same JSON with `status "error"`, `failure_reason "engine_lacks_op"`; **exit 1** both | PASS |

### Step 3g — library loop

| Call | Result | Verdict |
|---|---|---|
| MCP `summer_search_library {"query":"make the platformer jump feel better"}` | 14 ms, `semantic false` (lexical only), `count 8`. Top: `skill/celeste-momentum-platforming` (preview, 44.59), `skill/debugging-game-feel` (38.57), `skill/game-feel` (27.12), `skill/fps-controller` (21.01), `skill/design-mechanic` (18.22), `skill/make-game` (11.45)… | PASS |
| MCP `summer_read_library {"id":"skill/celeste-momentum-platforming"}` | 4 ms, 20,830 B. Header `skill/celeste-momentum-platforming — skill v0.1.1 (preview)`, `use_when`, `related: skill/design-mechanic, skill/game-feel, skill/debugging-game-feel`, `Invoke: the \`celeste-momentum-platforming\` skill`, body, `status: preview`, footer present verbatim: `— entry_id: skill/celeste-momentum-platforming@229b5e6d5fb9. If this entry is wrong, stale, or you deviate from it, report via summer_library_feedback.` | PASS |
| `SUMMER_NO_TELEMETRY=1` → `summer_library_feedback {"reports":[{"entry_id":"skill/celeste-momentum-platforming","outcome":"did_not_apply",…}],"engine_version":"0.5.65","agent_model":"claude-fable-5-1"}` | 2 ms, `{"recorded": false, "disabled": true}` | PASS |
| Real send (no env), `entry_id "skill/celeste-momentum-platforming@229b5e6d5fb9"`, `outcome "did_not_apply"`, `agent_model "claude-fable-5-1"` | 188 ms, **`{"recorded": false, "dropped": true}`**. Resent through the toolkit's own `sendLibraryFeedback` with `fetch` instrumented (status/body only): `POST https://www.summerengine.com/api/mcp/library-feedback` → **HTTP 404** `{"error":"not_found","message":"No API endpoint exists at POST /api/mcp/library-feedback.","hint":"Check the path and method. Machine-readable site index: https://www.summerengine.com/llms.txt","docs":"https://www.summerengine.com/docs","status":404}`. The web repo's `main` has no `src/app/api/mcp/` directory; the route exists only on branch `feat/library-feedback-mailbox` (STATUS.md L48/L53). | FAIL (F-03) |

### Step 3h — navigation

| Command | Result | Verdict |
|---|---|---|
| `summer open scene --print` | `{"op":"OpenScene","path":"res://scenes/rooms/room_01.tscn"}` / `Open a scene — The scene becomes the current tab in Summer Engine.` exit 0 — `path` auto-filled from the running project's main scene; identical with `--path res://scenes/rooms/room_01.tscn` | PASS |
| `summer open inspector --json` (live editor) | exit 0: `"action": "opened"`, `target {id "inspector", surface "editor", title "Inspector dock", status "implemented"}`, `op {"op":"FocusDock","dock":"inspector"}`, `engine {running true, version "0.5.65"}`, `receipt.results [{"dock":"inspector","ok":true,"op":"FocusDock"}]`, `terminalState "applied"`, `appliedSeq 50`. Screenshot after (`summer tool screenshot`) is the 2D viewport only, md5-identical to the pre-play frame — no tool can capture docks (`safety_boundary: summer_owned_viewport_texture`), so the dock focus is confirmable only by the receipt. | PASS |
| `summer open billing --print` | `https://www.summerengine.com/studio?tab=billing` / `Billing & plan — Current plan, upgrade, Stripe billing portal (payment method, invoices), top-ups.` exit 0 | PASS |
| `summer open generator` | `⚠ "generator" matches several destinations. Call again with one of the ids listed in matches.` then `generate-3d web 3D generation`, `generate-audio`, `generate-image`, `generate-video`; exit 1 | PASS |
| `summer open --list` | `Summer destinations (64)` — 58 `implemented`, 6 `planned` (`screen-2d`, `screen-3d`, `screen-script`, `screen-game`, `assistant`, `project-settings`) | PASS |

### Step 3i — doctor at the end

`summer doctor` → identical to step 2: 9 `✓`, `✗ Skills up to date  skills v2.5.0 are significantly behind v2.8.2`, `1 issue to fix.`, **exit 1**. `summer status` → `Auth: Logged in as …` / `API Token: Found (~/.summer/api-token)` / `Port: 6550` / `Engine: Running (v0.5.65)`, exit 0. **PARTIAL (F-09).**

## 2. Screenshots (`docs/design/e2e/`)

| File | What it is |
|---|---|
| `01-mcp-viewport-black.jpg` | 22 KB · the black `viewport` frame the MCP face returned at 16:51:25Z with "describe what you actually see" |
| `02-mcp-scene-render-pausemenu.jpg` | 61 KB · `target:"scene"` offscreen render of the 2D room: PauseMenu drawn open, plus the false "no Camera3D" warning |
| `03-cli-viewport-with-label.jpg` | 48 KB · CLI `viewport` 67 s later: the 2D editor view with `E2E 2026-09-03` at (200,100) |
| `04-mcp-viewport-preplay.jpg` | 31 KB · MCP `viewport` before `play` (1280x768) — also byte-identical to the post-`open inspector` capture |
| `05-mcp-game-frame.jpg` | 96 KB · `target:"game"` while running — real gameplay frame with the label persisted into the game |
| `06-probe-00_start.jpg` | 137 KB · `RunVerification` frame 0 (downscaled from 1920x1080) |
| `07-probe-02_waypoint_1.jpg` | 139 KB · `RunVerification` frame after waypoint 1: `+100` pickup text while the broken HUD still shows `x 0` |
| `mcp-drive.mjs` | the stdio MCP driver used for every MCP call above (`node mcp-drive.mjs --cwd <project> --out <dir> '<calls json>'`; supports `{"sleep_ms":N}` steps and `--env K=V`) |

## 3. Findings, ranked

P0 = a real agent would be blocked or misled · P1 = wrong but recoverable · P2 = polish.

| # | Sev | Finding | Evidence | Where |
|---|---|---|---|---|
| F-01 | **P0** | `summer_screenshot target:"viewport"` can return an all-black frame with no warning while the text instructs the agent to "Review the image above and describe what you actually see." An agent that has just added a node (the exact sequence here: mutation → tab switch 3D→2D → first capture) will describe a black scene and start "fixing" cameras/lights that are fine. 1 of 4 viewport captures; not root-caused (engine 0.5.65 capture vs. toolkit). The toolkit could at least detect a uniform frame and say so. | step 3c, `01-mcp-viewport-black.jpg` vs `03-…` | engine + `src/core/capabilities/tool-dispatch.ts` screenshot path |
| F-02 | **P0** | The pinned `2d-platformer` template boots with 4 runtime ERRORs (`hud.gd` `%HeartsContainer/%CoinsLabel/%ControlsHint/%HealthLabel` vs a `hud.tscn` with no `unique_name_in_owner`), so the very "Next step" `summer create` prints — `bash tests/autopilot/run.sh` — exits 1 with `FAILED: 6 engine error(s) during the run` on a pristine project, and every verification-ladder read (`get_debugger_errors`, `get_diagnostics`, probe `errors_seen`) is red before the user has changed anything. A first-time user concludes the install is broken; an agent chases template bugs instead of the user's task. | step 3e | `template-2d-platformer` @ `66fc71b8` (satellite repo) → needs a fix commit + pin bump in `library/templates/2d-platformer/resource.yaml` |
| F-03 | **P1** | Library feedback is dead against production: `POST https://www.summerengine.com/api/mcp/library-feedback` → 404 `No API endpoint exists at POST /api/mcp/library-feedback.`; the tool reports only `{"recorded": false, "dropped": true}` (contract: silent). Known — the route lives on unmerged web branch `feat/library-feedback-mailbox` (STATUS.md L48) and L53 already sequences it with npm 3.0.0 — but (a) the agent cannot tell "endpoint missing" from a network blip, and (b) the moment the CLI ships before the web branch, 100% of reports vanish silently. Becomes P0 if the publish order in STATUS.md slips. | step 3g | web repo route; `src/core/feedback/client.ts` could surface `status` in the dropped result |
| F-04 | **P1** | `get_project_context` — the tool every fresh chat is told to call first — is 88,803 B on the MCP face (≈22k tokens) and 144,441 B on the CLI face; the CLI face is **untrimmed** (969 settings, no `settingsTruncated/guidance/projectMemory`), so "same implementation on both faces" is not true for this tool. Nothing from `.summer/project.json` (template, commit, toolkit version) is surfaced in either. | step 2 | `src/cli/commands/tool.ts` vs MCP handler for `get-project-context` |
| F-05 | **P1** | `summer_screenshot target:"scene"` on a 2D scene appends `WARNING: this scene has no Camera3D — it will render grey/black when played.` (false — it played fine) and renders runtime-hidden UI (PauseMenu) as visible, so the "composition/framing" check misleads on 2D projects. | step 3c, `02-…jpg` | engine `ScenePreview` + toolkit warning text |
| F-06 | **P1** | CLI face exits **0** on a failed read: `summer tool inspect-node --args '{"path":"DoesNotExist"}'` prints `node not found: DoesNotExist` and exits 0, while the MCP face sets `isError true`. Scripts and agents using `summer tool` cannot detect failure by exit code. (`engine_lacks_op` and missing-argument cases *do* exit 1.) | step 3a control | `src/cli/commands/tool.ts` result → exit-code mapping |
| F-07 | **P1** | `summer_get_console` default filter dropped all 6 `std` lines (`droppedByLevel 6`) and reported `errors 0` right after a play session that produced 4 runtime errors (they live in the debugger). An agent that follows the skill text "read `summer_get_console` after a playthrough" alone sees a clean console. `get_diagnostics` routes correctly; the skill/playbook wording should not present `get_console` as sufficient. | step 3e | `library/skills/playtesting-a-feature/SKILL.md`, `summer_get_console` description |
| F-08 | **P1** | Template manifest lies about the content: summary says "TileMap levels … parallax backgrounds" platformer; the project is a Hollow Knight-style Metroidvania (`config/name="2D Metroidvania Template"`, `Polygon2D` geometry, 9 autoloads, quests, shop, boss). `template.json` says version `1.0.1` / name "2D Metroidvania Platformer" vs manifest `1.0.0`; its `CLAUDE.md` says "Godot 4.5 fork" and "Only 3 rooms exist" (7 ship). An agent that picks this template from search for a simple platformer gets a large game. | step 1 | `library/templates/2d-platformer/resource.yaml`, satellite `template.json`/`CLAUDE.md` |
| F-09 | **P1** | `summer doctor` exits 1 (`✗ Skills up to date`) on a machine where everything works, and its `recommendedAction` is `npx -y summer-engine@latest setup claude-code --yes --force`, which on a `--local-dev` machine would overwrite the checkout link with the published package. TESTING.md says "Exit 0 with warnings; only failures make it exit 1" — a version-skew condition is graded as a failure. | steps 0/2/3i | `src/cli/commands/doctor.ts` (`skills-version-stale` severity) |
| F-10 | **P1** | First open + first save produce a noisy, surprising git diff on a fresh template: the engine rewrote `project.godot` `config/features` `4.6`→`4.7`, left `project.godot.bak`, appended to `.gitignore`, and rewrote the whole `room_01.tscn` (136 lines for a 1-node add). Not the toolkit's code, but the first thing a `--keep-git` user sees in `git status`. | step 3b | engine 0.5.65 |
| F-11 | **P2** | `summer_run_editor_script` result: `checkpoint_capability_disabled` / `no_rewind_point true` by default — every editor-script run on a fresh project is non-rewindable and the tool tells the agent to "tell the user before doing more destructive work"; the mojibake in `no_rewind_reason` (`â`, double-encoded em dash) comes with it. | step 3d | engine string encoding; project-rewind default |
| F-12 | **P2** | Docs drift vs. reality: TESTING.md says `Summer tools (69)` (registry: 71); AGENTS.md §4 lists outcome words `worked, worked with fixes, wrong, outdated, incomplete` while the schema enum is `worked, worked_with_fixes, wrong, outdated, incomplete, did_not_apply, misrouted`. | steps 0, 3g | `docs/TESTING.md`, `AGENTS.md` |
| F-13 | **P2** | Argument naming is inconsistent across tools: node path is `path` on 17 tools, `nodePath` on 3 (`screenshot`, `select_node`, `set_resource_property`), `subjectPath` on 2; scene path is `scenePath` on 19 tools but `scene_path` on `world_snapshot`. Both faces reject wrong names clearly, but an agent working from AGENTS.md ("pass scenePath to …") guesses wrong on `inspect_node`, which has no `scenePath` at all. | step 3a | `library/tools/*/resource.yaml` schemas |
| F-14 | **P2** | `inspect_node` returns property `type` as a raw Godot `Variant.Type` integer (`2`, `20`, …) — the agent needs the enum table to read it. | step 3a | inspect payload shaping |
| F-15 | **P2** | Tool descriptions that contradict this engine: `summer_screenshot` says `"game"` is "Not available over a plain local connection — needs the Summer desktop app bridge" (it worked locally, 1.4 s); `summer_run_editor_script` says "tens of seconds" (2.3 s here). | steps 3d/3e | tool descriptions |
| F-16 | **P2** | `summer_test_placement`'s `engine_lacks_op` fallback tells the agent to use `summer_world_snapshot`, which is also `engine_lacks_op` on every shipped build. | step 3f | fallback text |
| F-17 | **P2** | The MCP `initialize` response carries no `instructions`; the tree read reports `activeTab "3D"` on a freshly opened 2D project; `doctor` says `Project Memory 0 files` although `.summer/project.json` exists (the pin is not counted as memory anywhere). | steps 2/3a | MCP server, memory summary |
| F-18 | **P2** | `summer_search_library` runs lexical-only (`semantic: false`) — documented, but the top hit for "make the platformer jump feel better" is a `preview` skill (`celeste-momentum-platforming` v0.1.1) ahead of the stable `debugging-game-feel`; `include_preview:false` is opt-in. | step 3g | ranking / default |

Things that were **right** and deserve saying: `create` pins and verifies exactly as documented (independent digest recompute matched); `run` was up in 3 s and the API answered on the first poll; every mutation returned `applied`/`SaveScene` receipts and persisted to disk; `run_editor_script` and `RunVerification` both ran in ~2 s with real frames; `play`/`stop`/`is_running` were consistent across faces; `engine_lacks_op` was structured and fast on both faces; the library read footer is exactly the reportable `entry_id`; `open` resolves and applies `FocusDock` with a receipt; ambiguous navigation fails with a list.

## 4. What a first-time user would have hit, in order

1. `summer create` — works, 1.7 s, digest verified. Good first impression.
2. `summer run` — editor up in 3 s. Good.
3. Runs the printed next step `bash tests/autopilot/run.sh` → **`FAILED: 6 engine error(s) during the run`** on the untouched template (F-02). They now believe the install or the engine is broken.
4. Asks the agent "add a label and screenshot it" → a real chance the agent receives a **black frame** and confidently describes/"fixes" a scene that is fine (F-01).
5. Asks "does the scene look right?" → the `scene` render shows an open pause menu and warns about a missing Camera3D on a 2D game (F-05).
6. `summer doctor` says **"1 issue to fix"** and exits 1 while everything works, then recommends a command that would replace their dev link (F-09).
7. `git status` on the template shows `project.godot` features bumped to 4.7, a `.bak`, and a fully rewritten scene (F-10).
8. Every fresh chat spends ≈22k tokens on `get_project_context` before doing anything (F-04).
9. Their agent's feedback reports all die at a 404 and nobody is told (F-03).

## 5. Not covered / caveats

- MCP face was driven by a custom stdio client, not Claude Code (the machine's real `$HOME`/`~/.claude` were out of scope, so `setup claude-code --local-dev` was not run).
- The first-run feedback disclosure could not be exercised (`~/.summer/feedback-first-run` already existed; `~/.summer` was read-only for this run).
- The black frame (F-01) was observed once and not root-caused; the capture metadata on the CLI face (`source_surface editor_2d_subviewport_texture`) suggests a not-yet-drawn 2D subviewport right after the tab switch.
- REVIEW/STATUS rows (TESTING.md §i) were **not** updated by this run; this document is the only artifact. Owner to fold F-01…F-18 into `REVIEW-<date>.md`/`STATUS.md`.
- No code was changed.

## 6. Cleanup (done)

1. `summer tool is-running` → `{"playing":false,"scene":""}` (game already stopped in step 3e).
2. `~/.summer/instances/9ebf351b….json` re-read: `pid 81460`, `resourceRoot <scratch>/summer-e2e-game` — matched the editor this run launched, so `kill 81460` (SIGTERM) was sent to that pid only; it exited after 2 s.
3. After: `pgrep -fl Summer.app/Contents/MacOS/Summer` → none (exit 1); `~/.summer/instances/` empty; the engine removed `~/.summer/api-token` and `api-port`; `summer status` → `Engine: Not running (no api-token found)`.
4. The scratch project `<scratch>/summer-e2e-game` was left in place (with `.git` detached at the pin, the `E2ELabel` edit, `tests/autopilot/out/`, and the engine's `.godot/` state) for inspection. No other Summer process, project, or `~/.summer` file was touched; `~/.claude` was not touched.
