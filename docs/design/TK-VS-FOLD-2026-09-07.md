# Toolkit v3 ↔ new engine (core fold) — first integration run, 2026-09-07

**Result: PASS, end to end, one scripted path.** The v3 toolkit (`v3-foundation` @ 32aa9da, CLI face = MCP face by parity) drove the core engine build (`fix/macos-no-focus-launch-fold` @ 33564c5, Godot 4.7.2) on a minimal local project, under an isolated HOME, engine launched `--summer-offscreen` with `SUMMER_WEBVIEW_EPHEMERAL=1`. Driver: `tk-vs-fold/drive.sh`; raw log `tk-vs-fold/drive.log`; key payloads alongside.

| Step | Result |
|---|---|
| Engine discovery | api-token published into isolated `~/.summer` in ~3 s; toolkit connected |
| `get-project-context` | health parsed: opKinds 117, runtimeControl.ops 16, `launchPostures ["focus","background","offscreen"]`; **capabilitySkewWarning** names 6 ops the engine lacks: Navigate, TestPlacement3D, SnapToSurface, AlignDistribute3D, NavigationProbe3D, Starcast3D |
| `open-scene`, `get-scene-tree` | applied / ok |
| **`run-script` (RunSceneScript)** | applied. The script created a `Label` "MitlProbe", authored a GDScript (`_ready` sets text, `_physics_process` increments `ticks`), attached it, saved. On disk: node present, script source embedded |
| `play` (quiet default) | applied; `is-running` ok |
| `get-runtime-tree /root/Main` | **MitlProbe present in the running game** |
| **`inspect-runtime-node /root/Main/MitlProbe`** | **`text = "MITL_ALIVE 263"` — the placed script is executing** |
| `world-snapshot` | applied, 1.3 KB structured snapshot |
| `game-probe` same-frame read | `MITL_ALIVE 285` |
| `get-diagnostics`, `screenshot game`, `stop` | ok / ok / applied |
| Teardown | 0 engines left; real `~/.summer` untouched; 0 `planted se_session`/`adopted web session` (ephemeral webview works); frontmost app unchanged (silent launch holds under load) |

## Findings
1. **Engine version stamp is still 0.5.65** on the fold; the toolkit's version fallback assumes 0.5.66+ for background launch. Must bump before release.
2. **Six ops missing from the fold** (exactly what the skew warning said): Navigate (`feat/navigate-op`), the spatial suite (PR #158), Starcast (PR #147). Plus headless worker (PR #155) which the toolkit's `src/core/headless/` layer targets. "Everything on one branch" = fold + #155 + #158 + #147 + navigate-op rehomed onto 4.7.2.
3. **Toolkit discovery is pointer-only without a project handle.** Attempt 1 launched with `--summer-no-publish`; the engine wrote only its instance-registry file and the toolkit reported "not running" on every call. `EngineApiClient.connect()` uses the registry only when given a selection (`--project`). Background/parallel instances need either the pointer or `--project`.
4. **`summer create` has no network timeout**: attempt 2 hung 42 min inside the template fetch.
5. **`summer run` has no engine-binary override**; it cannot launch a non-installed build. `SUMMER_BIN` is honored by the autopilot scaffold only.
6. One engine-side `ERROR: HTTPRequest is processing a request` during the run (engine's own HTTP client reuse); not toolkit-caused.
7. Two earlier attempts today failed on plumbing (3, 4), not on any tool. Per-tool behaviour was clean once the engine was reachable.

## Not covered
One scripted path, not the full TESTING.md recipe; no cold model choosing tools; no human session; single tiny project (no template import pass exercised here — that was covered by the pristine-boot matrix).

---

# Addendum 2026-09-09 — the COMPLETE engine (`core/all-engine-work` @ `436d0879ad8`, 0.5.66)

`core/all-engine-work` = the core fold + PR #147 (Starcast3D) + PR #158 (spatial suite + Marcus's companion fixes) + PR #155 (headless worker, `--summer-worker`) + the Navigate op (rebased onto 4.7.2) + version bump to 0.5.66 + the 8 new ops added to the health dispatch table. Every branch compiled against 4.7.2/clang `-Werror` for the first time (four small fixes). Logs in `tk-vs-fold/complete-build/`.

| Proof | Result |
|---|---|
| Toolkit drive (same script as above) vs complete build | PASS; **`capabilitySkewWarning: None`**, engine 0.5.66, opKinds 125; placed GDScript running (ticks 262); 0 engine errors |
| Engine e2e harness, run A (`--native`) | **1115 / 0 / 4** |
| Engine e2e harness, run B (`--native --trust-boot`, Blender 5.2) | **1175 / 0 / 1** |
| **Toolkit's own launch path**: `summer run --bin <complete> --background` | engine up in 1 s, v0.5.66, "will not take focus"; `summer doctor` ok 9 / warn 1 / fail 0 |
| Agent path through that launch | open-scene, run-script (placed code running: `RUNBIN 176`), play, game-probe, world-snapshot, `summer open inspector` (= Navigate op) all applied |
| 3D scene (`complete-build/scene3d.tscn`: floor + 2 crates + camera) | `test-placement` applied (2.6 KB report); `starcast` applied (4 KB); `navigation-probe` applied; `align-distribute-3d` applied (4.4 KB); `world-snapshot` applied; `screenshot target:scene framing:iso marks:true` ok; `snap-to-surface` → structured `gap_exceeds_hit_travel` (engine correctly refuses: crate already grounded). On the 2D probe the same ops returned `subject_not_node3d` / `subject_not_3d` — engine-side validation, not `engine_lacks_op` |
| Hygiene | 0 engines left after every run; real `~/.summer` untouched by the runs; 0 `planted se_session` / `adopted web session`; frontmost app never changed |

Toolkit plumbing fixed alongside (all on `v3-foundation`): registry-fallback discovery (`0597daa`, `c255296`), bounded `summer create` fetches (`b314e2f`), `summer run --bin` / `SUMMER_BIN` (`6a64157`), every test file under a fake HOME + real-`~/.summer` guard (`d904b3c`).

Still not proven: a cold model choosing tools from descriptions (MITL harness ready, needs a Claude login token); a human session; Windows launch posture; release build + Sparkle update path.

## Compat: published `summer-engine@2.8.2` MCP server vs the new engine (0.5.66) — 2026-09-09

Why this direction: the engine release (Sparkle auto-update) reaches users before `npm latest` flips, so the OLD toolkit must keep working against the NEW engine. Fetched the real `summer-engine-2.8.2.tgz` from npm, ran its `mcp` server over stdio against `core/all-engine-work` (offscreen, isolated HOME), calls in `tk-vs-fold/compat-2.8.2/calls.json`, log alongside.

| Step | Result |
|---|---|
| initialize | ok, serverInfo 2.8.2 |
| tools/list | 62 tools |
| `summer_get_project_context`, `summer_get_scene_tree`, `summer_add_node`, `summer_set_prop`, `summer_save_scene`, `summer_play`, `summer_is_running`, `summer_screenshot` (game), `summer_get_diagnostics`, `summer_stop` | **10/10 `isError: false`**, 21–257 ms each |
| on disk | node `OldMcp` present with its text; 0 engines left; real `~/.summer` untouched |

Note: 2.8.2 has no `summer tool <slug> --args` CLI face (that is v3); its agent surface is the MCP server, which is what was tested. UI polish commits (Asset Store hidden in agent view, skippable intro video) were fast-forwarded into `core/all-engine-work` (74f5063) after this run; they touch editor UI only.

## Final engine tip `core/all-engine-work` @ `899605c3110` — 2026-09-09

Adds on top of 436d087: editor-UI polish (Asset Store hidden in the agent layout — the old hide compared the label "AssetLib", renamed "Asset Store" in 4.7, so it never matched; skippable intro video from the second launch, reusing `summer_engine/play_intro_video`), Windows launch posture (`SW_SHOWNOACTIVATE`/`SWP_NOACTIVATE`, `WS_EX_TOOLWINDOW` for invisible, `WM_ACTIVATE` flips background→normal; `launchPostures` advertised on Windows), `tools/summer-cli` retired from the engine repo (generator now scans the toolkit repo as an external caller; phantom gate armed in CI), native modals suppressed by launch posture instead of an argv scan, and two test-only helpers guarded so the release configuration compiles warning-free. Release-config build (`cut_release.sh` flags: mono + Sparkle key) exits 0.

| Proof at 899605c | Result |
|---|---|
| Incremental build | exit 0, 0 warnings, 0.5.66 |
| Harness A | **1115 / 0 / 4** |
| Harness B (trust-boot + Blender) | **1175 / 0 / 1** |
| Toolkit drive | skew `None`, 125 ops, placed GDScript running (264 ticks) |
| Published 2.8.2 MCP vs this engine | 10/10 (see above) |

**Exit-time leak, pre-existing (not a regression):** with `--verbose`, a SIGTERM'd `--editor --summer-offscreen` session sometimes reports ~80 leaked instances (64 `TextLine`, `WebViewControl`, `StateProvider`, `OpsExecutor`, `AuthManager`, 2 `Image`, style boxes) and sometimes 6 — at 436d087 and at the tip alike (logs in `final-899605c/leak-verbose.log`). Summer-module singletons are not freed on a signal-driven shutdown. Filed as an engine follow-up; cosmetic in dev builds.
